---
name: code-review-simplify
description: 複雑な実装をシンプルにする方法を提案し、よりエレガントなコードへの改善案をGitHub PR形式で出力するスキル
---

# Code Review Skill: Simplify

Pull Requestのコードを分析し、**複雑な実装をよりシンプルにする方法**を提案するスキルです。実装方法の改善、ライブラリの活用、発想の転換により、理解しやすく保守しやすいコードへの改善案をGitHub PRに直接投稿可能な形式で提供します。

## 機能

このスキルは以下のタスクを実行します：

1. **PR情報の自動取得**
   - `gh`コマンドを使用して現在のブランチに関連するPRを特定
   - PRのdiff、変更ファイル一覧、既存のコメントを取得
   - 変更規模と影響範囲を分析

2. **複雑な実装の検出とシンプル化提案**
   - **実装方法の改善**: ロジックの単純化、不要な複雑性の削除
   - **ライブラリの活用**: 標準・サードパーティライブラリによる置き換え
   - **発想の転換**: 問題の再定義、アプローチの見直し
   - **固定概念の打破**: 別の視点からのシンプルな解決策

3. **具体的な改善提案**
   - 問題箇所の特定（ファイル名、行番号）
   - 重要度レベル（Critical / High / Medium / Low）
   - 具体的な修正コード例
   - 改善による効果の説明

4. **GitHub PR形式での出力**
   - ファイル・行番号を含むコメント形式
   - `gh pr review`コマンドで直接投稿可能
   - Markdown形式での見やすい整形
   - 優先度の高い順に整理

## レビュー観点：シンプリシティへの追求

このスキルの核心は「**複雑なものをシンプルにする**」ことです。以下の3つの観点から改善提案を行います。

### 1. 実装方法そのものをシンプルにする 🎯

複雑なロジックを見つけ、よりシンプルで理解しやすい実装方法を提案します。

#### チェックポイント

**a) 不要な複雑性の削除**
- 過剰な抽象化、将来使わない汎用化
- 読みにくい三項演算子の連鎖
- 不要な中間変数
- 複雑な条件式（De Morganの法則で簡略化）

**b) 制御フローの単純化**
- 深いネスト → 早期リターン / ガード節
- 複雑な条件分岐 → ポリモーフィズム / マップ構造
- ループ内の複雑な処理 → メソッドチェーン / 高階関数
- コールバック地獄 → async/await / Promise

**c) データ構造の見直し**
- 複数の変数 → オブジェクト / 構造体
- 複雑な配列操作 → 適切なデータ構造（Map, Set等）
- フラグ変数の乱用 → 状態遷移の明示化

**d) アルゴリズムの改善**
- O(n²) → O(n log n) または O(n)
- 同じ計算の繰り返し → メモ化
- 複雑な自前実装 → 既知のアルゴリズムパターン

#### 提案例

```javascript
// Before: 複雑なネストと条件分岐
function getDiscount(user, product, quantity) {
  if (user) {
    if (user.isPremium) {
      if (quantity > 10) {
        return product.price * 0.8;
      } else {
        return product.price * 0.9;
      }
    } else {
      if (quantity > 10) {
        return product.price * 0.95;
      }
    }
  }
  return product.price;
}

// After: 早期リターンでシンプル化
function getDiscount(user, product, quantity) {
  if (!user) return product.price;

  const baseDiscount = user.isPremium ? 0.1 : 0.05;
  const bulkDiscount = quantity > 10 ? 0.1 : 0;
  const totalDiscount = baseDiscount + bulkDiscount;

  return product.price * (1 - totalDiscount);
}
```

### 2. ライブラリを使ってシンプルに実装 📚

車輪の再発明を避け、既存のライブラリやフレームワーク機能を活用してシンプル化します。

#### チェックポイント

**a) 標準ライブラリの活用**
- 日付操作 → 標準の Date API / Temporal API
- 文字列処理 → String methods（`padStart`, `trim`, `split`等）
- 配列操作 → Array methods（`map`, `filter`, `reduce`, `flatMap`等）
- 非同期処理 → Promise API、async/await

**b) よく知られたサードパーティライブラリ**
- 日付/時刻 → date-fns, dayjs (重いmomentは避ける)
- HTTP通信 → axios, fetch API
- バリデーション → zod, yup, joi
- 状態管理 → zustand, jotai (複雑なReduxは避ける)
- ユーティリティ → lodash-es (必要な関数だけimport)

**c) フレームワーク/言語のビルトイン機能**
- React Hooks → useReducer, useCallback, useMemo
- TypeScript → type guards, discriminated unions
- Node.js → crypto, fs/promises, path
- CSS → Grid, Flexbox（複雑なJSレイアウトを避ける）

**d) 適切なライブラリ選択基準**
- バンドルサイズが小さい
- メンテナンスされている
- 学習コストが低い
- 過剰な機能を持たない

#### 提案例

```typescript
// Before: 日付操作を自前実装
function formatDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

function addDays(date: Date, days: number): Date {
  const result = new Date(date);
  result.setDate(result.getDate() + days);
  return result;
}

function isWeekend(date: Date): boolean {
  const day = date.getDay();
  return day === 0 || day === 6;
}

// After: date-fns を使ってシンプルに
import { format, addDays, isWeekend } from 'date-fns';

// 3つの関数が1行ずつで実現可能
const formatted = format(date, 'yyyy-MM-dd');
const futureDate = addDays(date, 7);
const weekend = isWeekend(date);
```

### 3. 発想を転換してシンプルに実装 💡

実装時の固定概念を取り払い、問題の捉え方や仕様を見直すことでシンプル化します。

#### チェックポイント

**a) 問題の再定義**
- 「本当に必要な機能か？」を問い直す
- 80/20の法則：20%の労力で80%の価値を実現
- エッジケースを無理に実装せず、制約として明示

**b) アプローチの見直し**
- プル型 ⇄ プッシュ型
- 同期 ⇄ 非同期
- クライアント処理 ⇄ サーバー処理
- 実装時計算 ⇄ 事前計算（キャッシュ、プリコンパイル）

**c) 仕様の簡略化提案**
- 「この条件分岐、本当に必要？」
- 完全な汎用性より、現在の要件に最適化
- 将来の拡張性を過度に考慮しない（YAGNI）

**d) 制約を受け入れる**
- 完璧を目指さず、実用的な範囲で十分
- パフォーマンスの過剰最適化を避ける
- エラーハンドリングは必要最小限に

#### 提案例

```typescript
// Before: 複雑なキャッシュ管理（完璧を目指しすぎ）
class DataCache {
  private cache = new Map<string, { data: any; timestamp: number; accessCount: number }>();
  private maxSize = 100;
  private ttl = 60000;

  get(key: string): any | null {
    const entry = this.cache.get(key);
    if (!entry) return null;

    if (Date.now() - entry.timestamp > this.ttl) {
      this.cache.delete(key);
      return null;
    }

    entry.accessCount++;
    return entry.data;
  }

  set(key: string, data: any): void {
    if (this.cache.size >= this.maxSize) {
      // LRU的な削除（複雑）
      let minAccess = Infinity;
      let keyToDelete = '';
      for (const [k, v] of this.cache.entries()) {
        if (v.accessCount < minAccess) {
          minAccess = v.accessCount;
          keyToDelete = k;
        }
      }
      this.cache.delete(keyToDelete);
    }

    this.cache.set(key, { data, timestamp: Date.now(), accessCount: 0 });
  }
}

// After: 発想を転換（シンプルなTTLキャッシュで十分）
// 問い: 本当にLRUやアクセスカウントが必要？単純なTTLで足りるのでは？
class DataCache {
  private cache = new Map<string, { data: any; expiresAt: number }>();

  get(key: string): any | null {
    const entry = this.cache.get(key);
    if (!entry || Date.now() > entry.expiresAt) {
      this.cache.delete(key);
      return null;
    }
    return entry.data;
  }

  set(key: string, data: any, ttlMs = 60000): void {
    this.cache.set(key, {
      data,
      expiresAt: Date.now() + ttlMs
    });
  }

  // 定期的に期限切れを削除（別途setIntervalで呼ぶ）
  cleanup(): void {
    const now = Date.now();
    for (const [key, value] of this.cache.entries()) {
      if (now > value.expiresAt) {
        this.cache.delete(key);
      }
    }
  }
}

// さらにシンプルに: そもそもキャッシュライブラリを使う
import { LRUCache } from 'lru-cache';
const cache = new LRUCache({ max: 100, ttl: 60000 });
```

## 重要度レベル定義

シンプル化の効果によって重要度を判定します：

- **🔴 Critical**: 著しく複雑で理解困難。今すぐシンプル化が必要（複雑度15以上、深いネスト、バグの温床）
- **🟠 High**: かなり複雑。早めにシンプル化すべき（複雑度10以上、車輪の再発明、ライブラリで代替可能）
- **🟡 Medium**: やや複雑。シンプル化を推奨（中程度の複雑度、もっと簡潔に書ける）
- **🟢 Low**: 小さな改善余地。時間があればシンプル化（軽微な最適化、よりエレガントな書き方の提案）

## 出力形式

### GitHub PR Review Comment 形式

```markdown
## コードレビュー結果：シンプル化提案

### 📊 変更概要
- 変更ファイル数: X件
- 追加行数: +XXX / 削除行数: -XXX
- 主な変更内容: [概要]

### 🎯 総合評価
- シンプリシティスコア: ★★★☆☆ (3/5)
- 主な強み: [良い点・シンプルな実装]
- シンプル化の余地: [複雑な箇所の要約]

---

### 🔴 Critical: 今すぐシンプル化が必要

#### `src/utils/validator.ts:45-89`
**問題**: 著しく複雑なバリデーション関数（循環的複雑度: 18）

**詳細**:
- 45行の関数に10個の条件分岐、4重ネスト
- 自前実装のバリデーションロジック
- テストが困難、バグの温床

**シンプル化提案（観点1: ライブラリ活用）**:
```typescript
// Before: 複雑な自前バリデーション（45行）
function validateUserInput(data: any): ValidationResult {
  const errors: string[] = [];

  if (!data.email) {
    errors.push('Email is required');
  } else {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(data.email)) {
      errors.push('Invalid email format');
    }
  }

  if (!data.age) {
    errors.push('Age is required');
  } else {
    if (typeof data.age !== 'number') {
      errors.push('Age must be a number');
    } else {
      if (data.age < 0 || data.age > 150) {
        errors.push('Age must be between 0 and 150');
      }
    }
  }

  // ... 他のフィールドも同様に続く ...

  return {
    isValid: errors.length === 0,
    errors
  };
}

// After: zod を使ってシンプルに（5行）
import { z } from 'zod';

const userSchema = z.object({
  email: z.string().email('Invalid email format'),
  age: z.number().min(0).max(150, 'Age must be between 0 and 150'),
  name: z.string().min(1, 'Name is required'),
  // 他のフィールドも宣言的に定義
});

// 使用時
const result = userSchema.safeParse(data);
// result.success, result.error で判定
```

**効果**:
- 45行 → 5行（90%削減）
- 宣言的で理解しやすい
- zodが型推論も提供
- メンテナンスが容易

---

### 🟠 High: 早めにシンプル化すべき

#### `src/components/DataTable.tsx:120-180`
**問題**: 日付フォーマット処理を自前実装（車輪の再発明）

**詳細**:
- 日付操作のユーティリティ関数を自作
- タイムゾーン処理が不完全
- エッジケースでバグの可能性

**シンプル化提案（観点2: ライブラリ活用）**:
```typescript
// Before: 自前実装（30行）
function formatDate(date: Date, format: string): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  // ... 複雑な文字列置換ロジック
}

function addBusinessDays(date: Date, days: number): Date {
  // ... 土日を考慮した複雑なロジック
}

// After: date-fns を使用（2行）
import { format, addBusinessDays } from 'date-fns';

const formatted = format(date, 'yyyy-MM-dd');
const futureDate = addBusinessDays(date, 5);
```

**効果**:
- 30行 → 2行
- テスト済みの信頼性の高いライブラリ
- タイムゾーン対応が正確

---

### 🟡 Medium: シンプル化を推奨

#### `src/services/api.ts:55-75`
**問題**: 複雑なネスト構造

**詳細**:
- 3重のif-else ネスト
- 早期リターンで簡潔にできる

**シンプル化提案（観点1: 実装方法の改善）**:
```typescript
// Before: 深いネスト
async function fetchUserData(id: string) {
  if (id) {
    const user = await getUser(id);
    if (user) {
      if (user.isActive) {
        return user;
      } else {
        throw new Error('User is not active');
      }
    } else {
      throw new Error('User not found');
    }
  } else {
    throw new Error('ID is required');
  }
}

// After: 早期リターンでフラット化
async function fetchUserData(id: string) {
  if (!id) throw new Error('ID is required');

  const user = await getUser(id);
  if (!user) throw new Error('User not found');
  if (!user.isActive) throw new Error('User is not active');

  return user;
}
```

**効果**: ネストが1段階に削減、読みやすさ向上

---

### 🟢 Low: よりエレガントな書き方

#### `src/utils/array.ts:10-15`
**問題**: 冗長な配列操作

**シンプル化提案（観点1: 実装方法の改善）**:
```typescript
// Before
const result = [];
for (let i = 0; i < items.length; i++) {
  if (items[i].isActive) {
    result.push(items[i].name);
  }
}

// After: メソッドチェーンでシンプルに
const result = items
  .filter(item => item.isActive)
  .map(item => item.name);
```

**効果**: 意図が明確、関数型プログラミングの恩恵

---

### 💡 発想の転換による提案

#### `src/hooks/usePolling.ts`
**問題**: 複雑なポーリング実装（50行）

**発想の転換（観点3）**:
現在の実装はクライアント側で定期的にポーリングしていますが、以下の方が シンプルでは？

**代替案1**: WebSocketを使う
- リアルタイム更新、ポーリングロジック不要
- サーバー側の実装が必要だが、クライアントはシンプル化

**代替案2**: Server-Sent Events (SSE)
- WebSocketより軽量、一方向通信で十分な場合に最適
- ポーリングより効率的

**代替案3**: ポーリング間隔を長くして要件を緩和
- 「本当に1秒ごとの更新が必要？5秒でも良いのでは？」
- 間隔を延ばせばシンプルな実装で十分

---

### ✅ 既にシンプルで良い点

- `src/components/Button.tsx`: シンプルで明確なprops設計
- `src/utils/format.ts`: 適切に関数分割されている
- `src/hooks/useAuth.ts`: 複雑性を隠蔽し、APIがシンプル

---

### 📝 シンプル化アクションプラン

1. **即対応** (Critical): `validator.ts` にzodを導入
2. **今週中** (High): `DataTable.tsx` にdate-fnsを導入
3. **次のスプリント** (Medium): ネスト構造をリファクタリング
4. **検討** (発想の転換): ポーリング→WebSocket移行の技術検証
```

## 使用方法

### 基本的な使い方

1. **レビュー対象のブランチに移動**
   ```bash
   git checkout feature/your-branch
   ```

2. **スキルを実行**
   - Claude Codeでスキルを呼び出す
   - 自動的にPR情報を取得し、レビューを開始

3. **レビュー結果の確認**
   - Markdown形式でレビュー結果が出力される
   - 重要度の高い順に問題点が整理される

4. **GitHub PRへのコメント投稿（オプション）**
   ```bash
   # レビュー結果をPRにコメントとして投稿
   gh pr review [PR番号] --comment --body "$(cat review-result.md)"
   ```

### カスタマイズ

レビューの焦点を調整する場合は、以下のような指示を追加できます：

- 「特にループ処理のシンプル化に注目してレビューしてください」
- 「車輪の再発明がないか、ライブラリで代替できる箇所を探してください」
- 「発想の転換で根本的にシンプルにできる箇所を提案してください」
- 「React hooksの使い方で、よりシンプルにできる方法を教えてください」

## 期待される効果

### 短期的効果
- **コード行数の削減**: 複雑な実装がシンプルになり、全体のコード量が減少
- **理解時間の短縮**: シンプルなコードは読みやすく、新メンバーのオンボーディングが加速
- **バグ混入リスク低減**: 複雑性が減ることでバグが入り込む余地が減少

### 長期的効果
- **保守コストの削減**: シンプルなコードは修正・拡張が容易
- **チームの生産性向上**: 既存ライブラリの活用スキルが向上
- **技術的負債の削減**: 過剰な抽象化や車輪の再発明を防止
- **発想力の養成**: 固定概念に囚われない柔軟な思考力が身につく
- **コードレビュー文化の醸成**: 「シンプルさ」を価値として共有

## 制限事項と注意点

### 制限事項
- **文脈依存の判断が困難**: ビジネス要件や過去の経緯を完全には理解できない
- **パフォーマンスは実測が必要**: シンプル≠高速の場合もあり、実測が必要
- **チーム規約との整合性**: チーム独自のルールや制約は考慮できない場合がある

### 注意点
- **シンプルすぎる提案**: 将来の拡張性を損なう可能性がある場合、人間が判断
- **ライブラリ追加のコスト**: バンドルサイズや学習コストも考慮が必要
- **仕様変更の提案**: 発想の転換による提案は、ステークホルダーとの調整が必要
- **最終判断は人間**: あくまで提案であり、採用するかはレビュアーが判断

## 補足情報

### 他のスキルとの連携

- **pr-learning**: レビュー後、優れたパターンを学習スキルで蓄積
- **組み合わせ使用**: レビュー → 修正 → 学習のサイクル構築

### 推奨ワークフロー

```
1. 機能実装（複雑になりがちな初期実装）
2. 動作確認・テスト
3. code-review スキル実行（シンプル化提案を受ける）
4. シンプル化の適用（Critical/High優先）
5. リファクタリング後の動作確認
6. 人間のレビュアーに依頼
7. 承認後、pr-learning で優れたパターンを学習
```

### シンプル化の原則

このスキルは以下の原則に基づいています：

> **"Simplicity is the ultimate sophistication."** - Leonardo da Vinci

- **KISS原則**: Keep It Simple, Stupid
- **YAGNI原則**: You Aren't Gonna Need It
- **車輪の再発明を避ける**: Don't Reinvent The Wheel
- **最小驚き原則**: Principle of Least Astonishment
- **オッカムの剃刀**: より単純な説明が正しい

このスキルを活用することで、**シンプルで美しく、保守しやすいコードベース**を構築できます。
